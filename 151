class Solution {
    public String reverseWords(String s) {
        String[] str=s.trim().split("\\s+");
        int n=str.length;
        for(int i=0;i<n/2;i++){
            String a=str[i];
            str[i]=str[n-i-1];
            str[n-i-1]=a;
        }
        return String.join(" ", str);
    }
}
